spring:
  application:
    name: kitchen-garden
  datasource:
    url: jdbc:h2:mem:kitchengarden;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  flyway:
    locations: classpath:db/migration